# DevTools — 在线开发工具集

## 项目概述

纯前端在线开发工具集合网站，所有数据仅在浏览器本地处理。单 HTML 文件架构，零依赖构建，开箱即用。

## 技术栈

- 纯 HTML + CSS + JavaScript（单文件）
- CodeMirror 5（CDN 引入，代码编辑器）
- 所有处理逻辑客户端完成，无后端

## 当前进度

> **更新时间**: 2026-06-28  
> **状态**: 所有 8 个工具模块已全部实现 🎉

### ✅ JSON 工具（已完成）

页面布局：左侧共享 JSON 输入编辑器 + 右侧操作按钮切换 + 结果面板

| 功能 | 说明 | 状态 |
|------|------|------|
| 格式化 | JSON 美化，支持 2空格/4空格/Tab 缩进 | ✅ |
| 压缩 | JSON 最小化，显示压缩前后大小对比、压缩率 | ✅ |
| 校验 | 合法性检查，显示错误位置、上下文、结构概览 | ✅ |
| 排序 | 递归按 key 排序，升序/降序可选 | ✅ |
| 转义 | JSON 字符串转义（escape）和去转义（unescape），逐字符处理无边界问题 | ✅ |
| 树视图 | 可折叠/展开的 JSON 树，显示数据类型，支持展开全部/折叠全部 | ✅ |

专用工具卡片（页面下方）：

| 功能 | 说明 | 状态 |
|------|------|------|
| JSON 比对 | 双编辑器输入，LCS算法行级 diff，支持语义比对（忽略key顺序） | ✅ |
| 格式转换 | JSON ↔ CSV / XML / YAML 双向转换 | ✅ |
| JSONPath | 支持 `$.key`、`[*]`、`..` 递归、过滤器 `[?(@.price>100)]`、切片、多索引 | ✅ |

### ✅ 编码解码（已完成）

**页面布局**：左侧共享输入 + 右侧编码/解码操作按钮 + 结果面板

| 功能 | 说明 |
|------|------|
| Base64 编码/解码 | 支持文本和文件的 Base64 编解码 |
| URL 编码/解码 | encodeURIComponent / decodeURIComponent |
| Unicode 转换 | `\uXXXX` ↔ 中文字符，支持批量转换 |
| HTML 实体编码/解码 | `&lt;` `&amp;` `&#xXXXX;` 等实体转换 |
| Hex 编码 | 字符串 ↔ 十六进制 |
| ASCII 码转换 | 字符 ↔ ASCII 码值对照表 |
| 图片 Base64 | 图片文件转 Base64 Data URL，支持预览 |

### ✅ 正则表达式测试（已完成）

**页面布局**：上方正则输入+标志位，左侧测试文本，右侧匹配结果高亮

| 功能 | 说明 |
|------|------|
| 正则实时测试 | 输入正则表达式，实时高亮匹配文本 |
| 标志位切换 | g / i / m / s / u 标志位独立开关 |
| 匹配结果列表 | 展示所有匹配项、位置、捕获分组 |
| 分组详细查看 | 每个捕获分组单独显示 |
| 替换功能 | 支持 $1 $& 等替换模式，预览替换结果 |
| 常用正则速查表 | 内置常用正则（邮箱/手机/URL/IP/身份证等） |

### ✅ 时间戳转换（已完成）

**页面布局**：左侧输入区，右侧实时转换结果

| 功能 | 说明 |
|------|------|
| 时间戳 → 日期 | Unix 秒/毫秒时间戳自动识别，转为可读日期 |
| 日期 → 时间戳 | 日期时间选择器，输出秒/毫秒时间戳 |
| 实时当前时间 | 实时刷新显示当前时间戳（秒+毫秒）和 UTC 时间 |
| 多时区对照 | 同时显示 UTC / 本地 / 常见时区时间 |
| ISO 8601 格式 | 生成和解析 ISO 8601 格式 |
| RFC 2822 格式 | 生成和解析 RFC 2822 格式 |

### ✅ 加密哈希（已完成）

**页面布局**：左侧输入区（文本或文件），右侧选择算法+输出结果

| 功能 | 说明 |
|------|------|
| MD5 哈希 | 字符串和文件的 MD5 计算 |
| SHA 系列 | SHA1 / SHA256 / SHA384 / SHA512 |
| HMAC | HMAC-MD5 / HMAC-SHA256，输入密钥 |
| AES 加解密 | AES-CBC / AES-GCM 模式，支持密码和密钥 |
| 文件哈希 | 拖拽文件计算哈希值，支持大文件分片 |
| 哈希比对 | 输入预期哈希值，自动比对验证 |

### ✅ 代码格式化（已完成）

**页面布局**：左侧共享输入，右侧操作按钮+格式化结果

| 功能 | 说明 |
|------|------|
| HTML 格式化/压缩 | 缩进美化 / 最小化压缩 |
| CSS 格式化/压缩 | 支持 CSS3，颜色值优化 |
| JavaScript 格式化 | 基于 prettier 风格的缩进和换行 |
| TypeScript 格式化 | 保留类型注解的格式化 |
| SQL 格式化 | 关键字大写/小写可选，缩进配置 |
| XML 格式化 | 与 JSON→XML 联动 |

### ✅ 字符串工具（已完成）

**页面布局**：左侧共享输入，右侧操作按钮+结果

| 功能 | 说明 |
|------|------|
| 大小写转换 | 大写 / 小写 / 首字母大写 / 驼峰 / 下划线互转 |
| 字符统计 | 字符数 / 单词数 / 行数 / 字节数（含中英文区分） |
| 排序 | 按行排序 / 按字母 / 按长度 / 随机打乱 |
| 去重 | 行去重，支持保留/移除空行和空白行 |
| 查找替换 | 支持正则表达式，高亮匹配项，预览替换 |
| 文本分割/合并 | 按分隔符/固定长度分割，多行合并 |
| 前后缀添加 | 每行添加前缀/后缀 |
| 行号添加 | 每行添加行号，支持自定义起始和格式 |

### ✅ 生成器（已完成）

**页面布局**：参数配置区 + 生成结果展示

| 功能 | 说明 |
|------|------|
| UUID 生成 | UUID v1 / v4，批量生成，复制单个或全部 |
| 随机密码 | 可配置长度、大小写字母、数字、特殊字符 |
| 二维码生成 | 文本/URL 生成二维码，支持调整尺寸和颜色，可下载 PNG |
| Lorem Ipsum | 生成指定段落/句子/单词数的占位文本 |
| 数字序列 | 生成等差数列、随机数，指定范围和步长 |
| 随机测试数据 | 随机姓名/邮箱/手机号/地址/身份证号等 |

---

## 扩展备选

适合后续扩展的工具方向：

| 类别 | 说明 |
|------|------|
| **网络工具** | IP 归属地查询、HTTP 状态码参考、User-Agent 解析、URL 解析器 |
| **颜色工具** | HEX ↔ RGB ↔ HSL 转换、调色板、渐变色生成 |
| **单位换算** | 长度/重量/温度/存储单位等 |
| **图片工具** | 图片压缩、格式转换、尺寸调整（需 Canvas API） |
| **Markdown 编辑器** | 实时预览的 Markdown 编辑器 |
| **Cron 表达式** | Cron 表达式解析和预览 |
| **JWT 解析** | JWT Token 解码查看 Header/Payload |
| **SQL 工具** | SQL 格式化/压缩、DDL 生成 |
| **文本差异对比** | 通用文本 diff（不限 JSON），类似 Beyond Compare |
| **流程图/图表** | Mermaid 图表在线编辑预览 |

---

## UI 设计规范

- **配色**：亮色/暗色双主题，CSS Variables 统一管理
- **布局**：左侧共享输入 + 右侧操作+结果（二栏布局）
- **编辑器**：CodeMirror 5，JSON mode 语法高亮
- **响应式**：≥900px 二栏布局，<900px 上下堆叠
- **交互**：Ctrl+Enter 快捷键执行当前操作
- **反馈**：Toast 消息提示，操作即时反馈
- **导航**：顶部一级 Tab 切换不同工具类别，支持横向滚动

## 文件结构

```
/home/jxw/IdeaProjects/tools/
└── index.html          # 主文件，包含所有 HTML/CSS/JS
```
